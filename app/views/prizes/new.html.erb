<%= form_for @prize, url: @prize.new_record? ? hackimoto_category_prizes_path : hackimoto_category_prize_path do |f| %>
  <%= f.label :rank %>:
  <%= f.text_field :rank %><br />

  <%= f.label :amount %>:
  <%= f.text_field :amount %><br />

  <%= f.submit %>
<% end %>