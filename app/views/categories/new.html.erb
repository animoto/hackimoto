<%= form_for @category, url: @category.new_record? ? hackimoto_categories_path : hackimoto_category_path do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %><br />

  <%= f.label :description %>:
  <%= f.text_field :description %><br />

  <%= f.submit %>
<% end %>